# 如何上传Logo和白皮书

## 📸 Logo上传指南

1. **准备logo文件**
   - 文件名：`logo.png`
   - 建议尺寸：512x512px 或 1024x1024px
   - 格式：PNG（支持透明背景）
   - 文件大小：建议小于500KB

2. **上传到GitHub**
   ```bash
   # 在本地项目中
   git add public/logo.png
   git commit -m "Add CyberHeart logo"
   git push
   ```

3. **使用位置**
   - 导航栏左侧
   - 页脚中心
   - 浏览器标签图标（favicon）
   - 社交媒体分享图

---

## 📄 白皮书上传指南

1. **准备白皮书文件**
   - 文件名：`litepaper.pdf`
   - 格式：PDF
   - 建议大小：小于10MB

2. **上传到GitHub**
   ```bash
   # 在本地项目中
   git add public/litepaper.pdf
   git commit -m "Add CyberHeart litepaper"
   git push
   ```

3. **链接位置**
   - Hero区域的"Read Litepaper (PDF)"按钮会自动链接到此文件

---

## 🔧 如果需要修改文件名

### 修改Logo文件名（例如改为 cyht-logo.png）

cd /home/project && npm_config_yes=true cd cyberheart && cat > public/README-UPLOAD.md << 'EOF'
# 如何上传Logo和白皮书

## 📸 Logo上传指南

1. **准备logo文件**
   - 文件名：`logo.png`
   - 建议尺寸：512x512px 或 1024x1024px
   - 格式：PNG（支持透明背景）
   - 文件大小：建议小于500KB

2. **上传到GitHub**
   ```bash
   # 在本地项目中
   git add public/logo.png
   git commit -m "Add CyberHeart logo"
   git push
   ```

3. **使用位置**
   - 导航栏左侧
   - 页脚中心
   - 浏览器标签图标（favicon）
   - 社交媒体分享图

---

## 📄 白皮书上传指南

1. **准备白皮书文件**
   - 文件名：`litepaper.pdf`
   - 格式：PDF
   - 建议大小：小于10MB

2. **上传到GitHub**
   ```bash
   # 在本地项目中
   git add public/litepaper.pdf
   git commit -m "Add CyberHeart litepaper"
   git push
   ```

3. **链接位置**
   - Hero区域的"Read Litepaper (PDF)"按钮会自动链接到此文件

---

## 🔧 如果需要修改文件名

### 修改Logo文件名（例如改为 cyht-logo.png）

`src/app/layout.tsx` 和 `src/app/page.tsx`

 `/logo.png` 替换为 `/cyht-logo.png`

### 修改白皮书文件名（例如改为 whitepaper.pdf）

cd /home/project && npm_config_yes=true cd cyberheart && cat > public/README-UPLOAD.md << 'EOF'
# 如何上传Logo和白皮书

## 📸 Logo上传指南

1. **准备logo文件**
   - 文件名：`logo.png`
   - 建议尺寸：512x512px 或 1024x1024px
   - 格式：PNG（支持透明背景）
   - 文件大小：建议小于500KB

2. **上传到GitHub**
   ```bash
   # 在本地项目中
   git add public/logo.png
   git commit -m "Add CyberHeart logo"
   git push
   ```

3. **使用位置**
   - 导航栏左侧
   - 页脚中心
   - 浏览器标签图标（favicon）
   - 社交媒体分享图

---

## 📄 白皮书上传指南

1. **准备白皮书文件**
   - 文件名：`litepaper.pdf`
   - 格式：PDF
   - 建议大小：小于10MB

2. **上传到GitHub**
   ```bash
   # 在本地项目中
   git add public/litepaper.pdf
   git commit -m "Add CyberHeart litepaper"
   git push
   ```

3. **链接位置**
   - Hero区域的"Read Litepaper (PDF)"按钮会自动链接到此文件

---

## 🔧 如果需要修改文件名

### 修改Logo文件名（例如改为 cyht-logo.png）

cd /home/project && npm_config_yes=true cd cyberheart && cat > public/README-UPLOAD.md << 'EOF'
# 如何上传Logo和白皮书

## 📸 Logo上传指南

1. **准备logo文件**
   - 文件名：`logo.png`
   - 建议尺寸：512x512px 或 1024x1024px
   - 格式：PNG（支持透明背景）
   - 文件大小：建议小于500KB

2. **上传到GitHub**
   ```bash
   # 在本地项目中
   git add public/logo.png
   git commit -m "Add CyberHeart logo"
   git push
   ```

3. **使用位置**
   - 导航栏左侧
   - 页脚中心
   - 浏览器标签图标（favicon）
   - 社交媒体分享图

---

## 📄 白皮书上传指南

1. **准备白皮书文件**
   - 文件名：`litepaper.pdf`
   - 格式：PDF
   - 建议大小：小于10MB

2. **上传到GitHub**
   ```bash
   # 在本地项目中
   git add public/litepaper.pdf
   git commit -m "Add CyberHeart litepaper"
   git push
   ```

3. **链接位置**
   - Hero区域的"Read Litepaper (PDF)"按钮会自动链接到此文件

---

## 🔧 如果需要修改文件名

### 修改Logo文件名（例如改为 cyht-logo.png）

`src/app/layout.tsx` 和 `src/app/page.tsx`

 `/logo.png` 替换为 `/cyht-logo.png`

### 修改白皮书文件名（例如改为 whitepaper.pdf）

`src/app/page.tsx`

cd /home/project && npm_config_yes=true cd cyberheart && cat > public/README-UPLOAD.md << 'EOF'
# 如何上传Logo和白皮书

## 📸 Logo上传指南

1. **准备logo文件**
   - 文件名：`logo.png`
   - 建议尺寸：512x512px 或 1024x1024px
   - 格式：PNG（支持透明背景）
   - 文件大小：建议小于500KB

2. **上传到GitHub**
   ```bash
   # 在本地项目中
   git add public/logo.png
   git commit -m "Add CyberHeart logo"
   git push
   ```

3. **使用位置**
   - 导航栏左侧
   - 页脚中心
   - 浏览器标签图标（favicon）
   - 社交媒体分享图

---

## 📄 白皮书上传指南

1. **准备白皮书文件**
   - 文件名：`litepaper.pdf`
   - 格式：PDF
   - 建议大小：小于10MB

2. **上传到GitHub**
   ```bash
   # 在本地项目中
   git add public/litepaper.pdf
   git commit -m "Add CyberHeart litepaper"
   git push
   ```

3. **链接位置**
   - Hero区域的"Read Litepaper (PDF)"按钮会自动链接到此文件

---

## 🔧 如果需要修改文件名

### 修改Logo文件名（例如改为 cyht-logo.png）

cd /home/project && npm_config_yes=true cd cyberheart && cat > public/README-UPLOAD.md << 'EOF'
# 如何上传Logo和白皮书

## 📸 Logo上传指南

1. **准备logo文件**
   - 文件名：`logo.png`
   - 建议尺寸：512x512px 或 1024x1024px
   - 格式：PNG（支持透明背景）
   - 文件大小：建议小于500KB

2. **上传到GitHub**
   ```bash
   # 在本地项目中
   git add public/logo.png
   git commit -m "Add CyberHeart logo"
   git push
   ```

3. **使用位置**
   - 导航栏左侧
   - 页脚中心
   - 浏览器标签图标（favicon）
   - 社交媒体分享图

---

## 📄 白皮书上传指南

1. **准备白皮书文件**
   - 文件名：`litepaper.pdf`
   - 格式：PDF
   - 建议大小：小于10MB

2. **上传到GitHub**
   ```bash
   # 在本地项目中
   git add public/litepaper.pdf
   git commit -m "Add CyberHeart litepaper"
   git push
   ```

3. **链接位置**
   - Hero区域的"Read Litepaper (PDF)"按钮会自动链接到此文件

---

## 🔧 如果需要修改文件名

### 修改Logo文件名（例如改为 cyht-logo.png）

`src/app/layout.tsx` 和 `src/app/page.tsx`

 `/logo.png` 替换为 `/cyht-logo.png`

### 修改白皮书文件名（例如改为 whitepaper.pdf）

cd /home/project && npm_config_yes=true cd cyberheart && cat > public/README-UPLOAD.md << 'EOF'
# 如何上传Logo和白皮书

## 📸 Logo上传指南

1. **准备logo文件**
   - 文件名：`logo.png`
   - 建议尺寸：512x512px 或 1024x1024px
   - 格式：PNG（支持透明背景）
   - 文件大小：建议小于500KB

2. **上传到GitHub**
   ```bash
   # 在本地项目中
   git add public/logo.png
   git commit -m "Add CyberHeart logo"
   git push
   ```

3. **使用位置**
   - 导航栏左侧
   - 页脚中心
   - 浏览器标签图标（favicon）
   - 社交媒体分享图

---

## 📄 白皮书上传指南

1. **准备白皮书文件**
   - 文件名：`litepaper.pdf`
   - 格式：PDF
   - 建议大小：小于10MB

2. **上传到GitHub**
   ```bash
   # 在本地项目中
   git add public/litepaper.pdf
   git commit -m "Add CyberHeart litepaper"
   git push
   ```

3. **链接位置**
   - Hero区域的"Read Litepaper (PDF)"按钮会自动链接到此文件

---

## 🔧 如果需要修改文件名

### 修改Logo文件名（例如改为 cyht-logo.png）

cd /home/project && npm_config_yes=true cd cyberheart && cat > public/README-UPLOAD.md << 'EOF'
# 如何上传Logo和白皮书

## 📸 Logo上传指南

1. **准备logo文件**
   - 文件名：`logo.png`
   - 建议尺寸：512x512px 或 1024x1024px
   - 格式：PNG（支持透明背景）
   - 文件大小：建议小于500KB

2. **上传到GitHub**
   ```bash
   # 在本地项目中
   git add public/logo.png
   git commit -m "Add CyberHeart logo"
   git push
   ```

3. **使用位置**
   - 导航栏左侧
   - 页脚中心
   - 浏览器标签图标（favicon）
   - 社交媒体分享图

---

## 📄 白皮书上传指南

1. **准备白皮书文件**
   - 文件名：`litepaper.pdf`
   - 格式：PDF
   - 建议大小：小于10MB

2. **上传到GitHub**
   ```bash
   # 在本地项目中
   git add public/litepaper.pdf
   git commit -m "Add CyberHeart litepaper"
   git push
   ```

3. **链接位置**
   - Hero区域的"Read Litepaper (PDF)"按钮会自动链接到此文件

---

## 🔧 如果需要修改文件名

### 修改Logo文件名（例如改为 cyht-logo.png）

`src/app/layout.tsx` 和 `src/app/page.tsx`

 `/logo.png` 替换为 `/cyht-logo.png`

### 修改白皮书文件名（例如改为 whitepaper.pdf）

`src/app/page.tsx`


```tsx
<a href="/litepaper.pdf" target="_blank" rel="noopener noreferrer">
```

cd /home/project && npm_config_yes=true cd cyberheart && cat > public/README-UPLOAD.md << 'EOF'
# 如何上传Logo和白皮书

## 📸 Logo上传指南

1. **准备logo文件**
   - 文件名：`logo.png`
   - 建议尺寸：512x512px 或 1024x1024px
   - 格式：PNG（支持透明背景）
   - 文件大小：建议小于500KB

2. **上传到GitHub**
   ```bash
   # 在本地项目中
   git add public/logo.png
   git commit -m "Add CyberHeart logo"
   git push
   ```

3. **使用位置**
   - 导航栏左侧
   - 页脚中心
   - 浏览器标签图标（favicon）
   - 社交媒体分享图

---

## 📄 白皮书上传指南

1. **准备白皮书文件**
   - 文件名：`litepaper.pdf`
   - 格式：PDF
   - 建议大小：小于10MB

2. **上传到GitHub**
   ```bash
   # 在本地项目中
   git add public/litepaper.pdf
   git commit -m "Add CyberHeart litepaper"
   git push
   ```

3. **链接位置**
   - Hero区域的"Read Litepaper (PDF)"按钮会自动链接到此文件

---

## 🔧 如果需要修改文件名

### 修改Logo文件名（例如改为 cyht-logo.png）

cd /home/project && npm_config_yes=true cd cyberheart && cat > public/README-UPLOAD.md << 'EOF'
# 如何上传Logo和白皮书

## 📸 Logo上传指南

1. **准备logo文件**
   - 文件名：`logo.png`
   - 建议尺寸：512x512px 或 1024x1024px
   - 格式：PNG（支持透明背景）
   - 文件大小：建议小于500KB

2. **上传到GitHub**
   ```bash
   # 在本地项目中
   git add public/logo.png
   git commit -m "Add CyberHeart logo"
   git push
   ```

3. **使用位置**
   - 导航栏左侧
   - 页脚中心
   - 浏览器标签图标（favicon）
   - 社交媒体分享图

---

## 📄 白皮书上传指南

1. **准备白皮书文件**
   - 文件名：`litepaper.pdf`
   - 格式：PDF
   - 建议大小：小于10MB

2. **上传到GitHub**
   ```bash
   # 在本地项目中
   git add public/litepaper.pdf
   git commit -m "Add CyberHeart litepaper"
   git push
   ```

3. **链接位置**
   - Hero区域的"Read Litepaper (PDF)"按钮会自动链接到此文件

---

## 🔧 如果需要修改文件名

### 修改Logo文件名（例如改为 cyht-logo.png）

`src/app/layout.tsx` 和 `src/app/page.tsx`

 `/logo.png` 替换为 `/cyht-logo.png`

### 修改白皮书文件名（例如改为 whitepaper.pdf）

cd /home/project && npm_config_yes=true cd cyberheart && cat > public/README-UPLOAD.md << 'EOF'
# 如何上传Logo和白皮书

## 📸 Logo上传指南

1. **准备logo文件**
   - 文件名：`logo.png`
   - 建议尺寸：512x512px 或 1024x1024px
   - 格式：PNG（支持透明背景）
   - 文件大小：建议小于500KB

2. **上传到GitHub**
   ```bash
   # 在本地项目中
   git add public/logo.png
   git commit -m "Add CyberHeart logo"
   git push
   ```

3. **使用位置**
   - 导航栏左侧
   - 页脚中心
   - 浏览器标签图标（favicon）
   - 社交媒体分享图

---

## 📄 白皮书上传指南

1. **准备白皮书文件**
   - 文件名：`litepaper.pdf`
   - 格式：PDF
   - 建议大小：小于10MB

2. **上传到GitHub**
   ```bash
   # 在本地项目中
   git add public/litepaper.pdf
   git commit -m "Add CyberHeart litepaper"
   git push
   ```

3. **链接位置**
   - Hero区域的"Read Litepaper (PDF)"按钮会自动链接到此文件

---

## 🔧 如果需要修改文件名

### 修改Logo文件名（例如改为 cyht-logo.png）

cd /home/project && npm_config_yes=true cd cyberheart && cat > public/README-UPLOAD.md << 'EOF'
# 如何上传Logo和白皮书

## 📸 Logo上传指南

1. **准备logo文件**
   - 文件名：`logo.png`
   - 建议尺寸：512x512px 或 1024x1024px
   - 格式：PNG（支持透明背景）
   - 文件大小：建议小于500KB

2. **上传到GitHub**
   ```bash
   # 在本地项目中
   git add public/logo.png
   git commit -m "Add CyberHeart logo"
   git push
   ```

3. **使用位置**
   - 导航栏左侧
   - 页脚中心
   - 浏览器标签图标（favicon）
   - 社交媒体分享图

---

## 📄 白皮书上传指南

1. **准备白皮书文件**
   - 文件名：`litepaper.pdf`
   - 格式：PDF
   - 建议大小：小于10MB

2. **上传到GitHub**
   ```bash
   # 在本地项目中
   git add public/litepaper.pdf
   git commit -m "Add CyberHeart litepaper"
   git push
   ```

3. **链接位置**
   - Hero区域的"Read Litepaper (PDF)"按钮会自动链接到此文件

---

## 🔧 如果需要修改文件名

### 修改Logo文件名（例如改为 cyht-logo.png）

`src/app/layout.tsx` 和 `src/app/page.tsx`

 `/logo.png` 替换为 `/cyht-logo.png`

### 修改白皮书文件名（例如改为 whitepaper.pdf）

`src/app/page.tsx`

cd /home/project && npm_config_yes=true cd cyberheart && cat > public/README-UPLOAD.md << 'EOF'
# 如何上传Logo和白皮书

## 📸 Logo上传指南

1. **准备logo文件**
   - 文件名：`logo.png`
   - 建议尺寸：512x512px 或 1024x1024px
   - 格式：PNG（支持透明背景）
   - 文件大小：建议小于500KB

2. **上传到GitHub**
   ```bash
   # 在本地项目中
   git add public/logo.png
   git commit -m "Add CyberHeart logo"
   git push
   ```

3. **使用位置**
   - 导航栏左侧
   - 页脚中心
   - 浏览器标签图标（favicon）
   - 社交媒体分享图

---

## 📄 白皮书上传指南

1. **准备白皮书文件**
   - 文件名：`litepaper.pdf`
   - 格式：PDF
   - 建议大小：小于10MB

2. **上传到GitHub**
   ```bash
   # 在本地项目中
   git add public/litepaper.pdf
   git commit -m "Add CyberHeart litepaper"
   git push
   ```

3. **链接位置**
   - Hero区域的"Read Litepaper (PDF)"按钮会自动链接到此文件

---

## 🔧 如果需要修改文件名

### 修改Logo文件名（例如改为 cyht-logo.png）

cd /home/project && npm_config_yes=true cd cyberheart && cat > public/README-UPLOAD.md << 'EOF'
# 如何上传Logo和白皮书

## 📸 Logo上传指南

1. **准备logo文件**
   - 文件名：`logo.png`
   - 建议尺寸：512x512px 或 1024x1024px
   - 格式：PNG（支持透明背景）
   - 文件大小：建议小于500KB

2. **上传到GitHub**
   ```bash
   # 在本地项目中
   git add public/logo.png
   git commit -m "Add CyberHeart logo"
   git push
   ```

3. **使用位置**
   - 导航栏左侧
   - 页脚中心
   - 浏览器标签图标（favicon）
   - 社交媒体分享图

---

## 📄 白皮书上传指南

1. **准备白皮书文件**
   - 文件名：`litepaper.pdf`
   - 格式：PDF
   - 建议大小：小于10MB

2. **上传到GitHub**
   ```bash
   # 在本地项目中
   git add public/litepaper.pdf
   git commit -m "Add CyberHeart litepaper"
   git push
   ```

3. **链接位置**
   - Hero区域的"Read Litepaper (PDF)"按钮会自动链接到此文件

---

## 🔧 如果需要修改文件名

### 修改Logo文件名（例如改为 cyht-logo.png）

`src/app/layout.tsx` 和 `src/app/page.tsx`

 `/logo.png` 替换为 `/cyht-logo.png`

### 修改白皮书文件名（例如改为 whitepaper.pdf）

cd /home/project && npm_config_yes=true cd cyberheart && cat > public/README-UPLOAD.md << 'EOF'
# 如何上传Logo和白皮书

## 📸 Logo上传指南

1. **准备logo文件**
   - 文件名：`logo.png`
   - 建议尺寸：512x512px 或 1024x1024px
   - 格式：PNG（支持透明背景）
   - 文件大小：建议小于500KB

2. **上传到GitHub**
   ```bash
   # 在本地项目中
   git add public/logo.png
   git commit -m "Add CyberHeart logo"
   git push
   ```

3. **使用位置**
   - 导航栏左侧
   - 页脚中心
   - 浏览器标签图标（favicon）
   - 社交媒体分享图

---

## 📄 白皮书上传指南

1. **准备白皮书文件**
   - 文件名：`litepaper.pdf`
   - 格式：PDF
   - 建议大小：小于10MB

2. **上传到GitHub**
   ```bash
   # 在本地项目中
   git add public/litepaper.pdf
   git commit -m "Add CyberHeart litepaper"
   git push
   ```

3. **链接位置**
   - Hero区域的"Read Litepaper (PDF)"按钮会自动链接到此文件

---

## 🔧 如果需要修改文件名

### 修改Logo文件名（例如改为 cyht-logo.png）

cd /home/project && npm_config_yes=true cd cyberheart && cat > public/README-UPLOAD.md << 'EOF'
# 如何上传Logo和白皮书

## 📸 Logo上传指南

1. **准备logo文件**
   - 文件名：`logo.png`
   - 建议尺寸：512x512px 或 1024x1024px
   - 格式：PNG（支持透明背景）
   - 文件大小：建议小于500KB

2. **上传到GitHub**
   ```bash
   # 在本地项目中
   git add public/logo.png
   git commit -m "Add CyberHeart logo"
   git push
   ```

3. **使用位置**
   - 导航栏左侧
   - 页脚中心
   - 浏览器标签图标（favicon）
   - 社交媒体分享图

---

## 📄 白皮书上传指南

1. **准备白皮书文件**
   - 文件名：`litepaper.pdf`
   - 格式：PDF
   - 建议大小：小于10MB

2. **上传到GitHub**
   ```bash
   # 在本地项目中
   git add public/litepaper.pdf
   git commit -m "Add CyberHeart litepaper"
   git push
   ```

3. **链接位置**
   - Hero区域的"Read Litepaper (PDF)"按钮会自动链接到此文件

---

## 🔧 如果需要修改文件名

### 修改Logo文件名（例如改为 cyht-logo.png）

`src/app/layout.tsx` 和 `src/app/page.tsx`

 `/logo.png` 替换为 `/cyht-logo.png`

### 修改白皮书文件名（例如改为 whitepaper.pdf）

`src/app/page.tsx`


```tsx
<a href="/litepaper.pdf" target="_blank" rel="noopener noreferrer">
```


```tsx
<a href="/whitepaper.pdf" target="_blank" rel="noopener noreferrer">
```

---

## 📁 文件位置

cd /home/project && npm_config_yes=true cd cyberheart && cat > public/README-UPLOAD.md << 'EOF'
# 如何上传Logo和白皮书

## 📸 Logo上传指南

1. **准备logo文件**
   - 文件名：`logo.png`
   - 建议尺寸：512x512px 或 1024x1024px
   - 格式：PNG（支持透明背景）
   - 文件大小：建议小于500KB

2. **上传到GitHub**
   ```bash
   # 在本地项目中
   git add public/logo.png
   git commit -m "Add CyberHeart logo"
   git push
   ```

3. **使用位置**
   - 导航栏左侧
   - 页脚中心
   - 浏览器标签图标（favicon）
   - 社交媒体分享图

---

## 📄 白皮书上传指南

1. **准备白皮书文件**
   - 文件名：`litepaper.pdf`
   - 格式：PDF
   - 建议大小：小于10MB

2. **上传到GitHub**
   ```bash
   # 在本地项目中
   git add public/litepaper.pdf
   git commit -m "Add CyberHeart litepaper"
   git push
   ```

3. **链接位置**
   - Hero区域的"Read Litepaper (PDF)"按钮会自动链接到此文件

---

## 🔧 如果需要修改文件名

### 修改Logo文件名（例如改为 cyht-logo.png）

cd /home/project && npm_config_yes=true cd cyberheart && cat > public/README-UPLOAD.md << 'EOF'
# 如何上传Logo和白皮书

## 📸 Logo上传指南

1. **准备logo文件**
   - 文件名：`logo.png`
   - 建议尺寸：512x512px 或 1024x1024px
   - 格式：PNG（支持透明背景）
   - 文件大小：建议小于500KB

2. **上传到GitHub**
   ```bash
   # 在本地项目中
   git add public/logo.png
   git commit -m "Add CyberHeart logo"
   git push
   ```

3. **使用位置**
   - 导航栏左侧
   - 页脚中心
   - 浏览器标签图标（favicon）
   - 社交媒体分享图

---

## 📄 白皮书上传指南

1. **准备白皮书文件**
   - 文件名：`litepaper.pdf`
   - 格式：PDF
   - 建议大小：小于10MB

2. **上传到GitHub**
   ```bash
   # 在本地项目中
   git add public/litepaper.pdf
   git commit -m "Add CyberHeart litepaper"
   git push
   ```

3. **链接位置**
   - Hero区域的"Read Litepaper (PDF)"按钮会自动链接到此文件

---

## 🔧 如果需要修改文件名

### 修改Logo文件名（例如改为 cyht-logo.png）

`src/app/layout.tsx` 和 `src/app/page.tsx`

 `/logo.png` 替换为 `/cyht-logo.png`

### 修改白皮书文件名（例如改为 whitepaper.pdf）

cd /home/project && npm_config_yes=true cd cyberheart && cat > public/README-UPLOAD.md << 'EOF'
# 如何上传Logo和白皮书

## 📸 Logo上传指南

1. **准备logo文件**
   - 文件名：`logo.png`
   - 建议尺寸：512x512px 或 1024x1024px
   - 格式：PNG（支持透明背景）
   - 文件大小：建议小于500KB

2. **上传到GitHub**
   ```bash
   # 在本地项目中
   git add public/logo.png
   git commit -m "Add CyberHeart logo"
   git push
   ```

3. **使用位置**
   - 导航栏左侧
   - 页脚中心
   - 浏览器标签图标（favicon）
   - 社交媒体分享图

---

## 📄 白皮书上传指南

1. **准备白皮书文件**
   - 文件名：`litepaper.pdf`
   - 格式：PDF
   - 建议大小：小于10MB

2. **上传到GitHub**
   ```bash
   # 在本地项目中
   git add public/litepaper.pdf
   git commit -m "Add CyberHeart litepaper"
   git push
   ```

3. **链接位置**
   - Hero区域的"Read Litepaper (PDF)"按钮会自动链接到此文件

---

## 🔧 如果需要修改文件名

### 修改Logo文件名（例如改为 cyht-logo.png）

cd /home/project && npm_config_yes=true cd cyberheart && cat > public/README-UPLOAD.md << 'EOF'
# 如何上传Logo和白皮书

## 📸 Logo上传指南

1. **准备logo文件**
   - 文件名：`logo.png`
   - 建议尺寸：512x512px 或 1024x1024px
   - 格式：PNG（支持透明背景）
   - 文件大小：建议小于500KB

2. **上传到GitHub**
   ```bash
   # 在本地项目中
   git add public/logo.png
   git commit -m "Add CyberHeart logo"
   git push
   ```

3. **使用位置**
   - 导航栏左侧
   - 页脚中心
   - 浏览器标签图标（favicon）
   - 社交媒体分享图

---

## 📄 白皮书上传指南

1. **准备白皮书文件**
   - 文件名：`litepaper.pdf`
   - 格式：PDF
   - 建议大小：小于10MB

2. **上传到GitHub**
   ```bash
   # 在本地项目中
   git add public/litepaper.pdf
   git commit -m "Add CyberHeart litepaper"
   git push
   ```

3. **链接位置**
   - Hero区域的"Read Litepaper (PDF)"按钮会自动链接到此文件

---

## 🔧 如果需要修改文件名

### 修改Logo文件名（例如改为 cyht-logo.png）

`src/app/layout.tsx` 和 `src/app/page.tsx`

 `/logo.png` 替换为 `/cyht-logo.png`

### 修改白皮书文件名（例如改为 whitepaper.pdf）

`src/app/page.tsx`

cd /home/project && npm_config_yes=true cd cyberheart && cat > public/README-UPLOAD.md << 'EOF'
# 如何上传Logo和白皮书

## 📸 Logo上传指南

1. **准备logo文件**
   - 文件名：`logo.png`
   - 建议尺寸：512x512px 或 1024x1024px
   - 格式：PNG（支持透明背景）
   - 文件大小：建议小于500KB

2. **上传到GitHub**
   ```bash
   # 在本地项目中
   git add public/logo.png
   git commit -m "Add CyberHeart logo"
   git push
   ```

3. **使用位置**
   - 导航栏左侧
   - 页脚中心
   - 浏览器标签图标（favicon）
   - 社交媒体分享图

---

## 📄 白皮书上传指南

1. **准备白皮书文件**
   - 文件名：`litepaper.pdf`
   - 格式：PDF
   - 建议大小：小于10MB

2. **上传到GitHub**
   ```bash
   # 在本地项目中
   git add public/litepaper.pdf
   git commit -m "Add CyberHeart litepaper"
   git push
   ```

3. **链接位置**
   - Hero区域的"Read Litepaper (PDF)"按钮会自动链接到此文件

---

## 🔧 如果需要修改文件名

### 修改Logo文件名（例如改为 cyht-logo.png）

cd /home/project && npm_config_yes=true cd cyberheart && cat > public/README-UPLOAD.md << 'EOF'
# 如何上传Logo和白皮书

## 📸 Logo上传指南

1. **准备logo文件**
   - 文件名：`logo.png`
   - 建议尺寸：512x512px 或 1024x1024px
   - 格式：PNG（支持透明背景）
   - 文件大小：建议小于500KB

2. **上传到GitHub**
   ```bash
   # 在本地项目中
   git add public/logo.png
   git commit -m "Add CyberHeart logo"
   git push
   ```

3. **使用位置**
   - 导航栏左侧
   - 页脚中心
   - 浏览器标签图标（favicon）
   - 社交媒体分享图

---

## 📄 白皮书上传指南

1. **准备白皮书文件**
   - 文件名：`litepaper.pdf`
   - 格式：PDF
   - 建议大小：小于10MB

2. **上传到GitHub**
   ```bash
   # 在本地项目中
   git add public/litepaper.pdf
   git commit -m "Add CyberHeart litepaper"
   git push
   ```

3. **链接位置**
   - Hero区域的"Read Litepaper (PDF)"按钮会自动链接到此文件

---

## 🔧 如果需要修改文件名

### 修改Logo文件名（例如改为 cyht-logo.png）

`src/app/layout.tsx` 和 `src/app/page.tsx`

 `/logo.png` 替换为 `/cyht-logo.png`

### 修改白皮书文件名（例如改为 whitepaper.pdf）

cd /home/project && npm_config_yes=true cd cyberheart && cat > public/README-UPLOAD.md << 'EOF'
# 如何上传Logo和白皮书

## 📸 Logo上传指南

1. **准备logo文件**
   - 文件名：`logo.png`
   - 建议尺寸：512x512px 或 1024x1024px
   - 格式：PNG（支持透明背景）
   - 文件大小：建议小于500KB

2. **上传到GitHub**
   ```bash
   # 在本地项目中
   git add public/logo.png
   git commit -m "Add CyberHeart logo"
   git push
   ```

3. **使用位置**
   - 导航栏左侧
   - 页脚中心
   - 浏览器标签图标（favicon）
   - 社交媒体分享图

---

## 📄 白皮书上传指南

1. **准备白皮书文件**
   - 文件名：`litepaper.pdf`
   - 格式：PDF
   - 建议大小：小于10MB

2. **上传到GitHub**
   ```bash
   # 在本地项目中
   git add public/litepaper.pdf
   git commit -m "Add CyberHeart litepaper"
   git push
   ```

3. **链接位置**
   - Hero区域的"Read Litepaper (PDF)"按钮会自动链接到此文件

---

## 🔧 如果需要修改文件名

### 修改Logo文件名（例如改为 cyht-logo.png）

cd /home/project && npm_config_yes=true cd cyberheart && cat > public/README-UPLOAD.md << 'EOF'
# 如何上传Logo和白皮书

## 📸 Logo上传指南

1. **准备logo文件**
   - 文件名：`logo.png`
   - 建议尺寸：512x512px 或 1024x1024px
   - 格式：PNG（支持透明背景）
   - 文件大小：建议小于500KB

2. **上传到GitHub**
   ```bash
   # 在本地项目中
   git add public/logo.png
   git commit -m "Add CyberHeart logo"
   git push
   ```

3. **使用位置**
   - 导航栏左侧
   - 页脚中心
   - 浏览器标签图标（favicon）
   - 社交媒体分享图

---

## 📄 白皮书上传指南

1. **准备白皮书文件**
   - 文件名：`litepaper.pdf`
   - 格式：PDF
   - 建议大小：小于10MB

2. **上传到GitHub**
   ```bash
   # 在本地项目中
   git add public/litepaper.pdf
   git commit -m "Add CyberHeart litepaper"
   git push
   ```

3. **链接位置**
   - Hero区域的"Read Litepaper (PDF)"按钮会自动链接到此文件

---

## 🔧 如果需要修改文件名

### 修改Logo文件名（例如改为 cyht-logo.png）

`src/app/layout.tsx` 和 `src/app/page.tsx`

 `/logo.png` 替换为 `/cyht-logo.png`

### 修改白皮书文件名（例如改为 whitepaper.pdf）

`src/app/page.tsx`


```tsx
<a href="/litepaper.pdf" target="_blank" rel="noopener noreferrer">
```

cd /home/project && npm_config_yes=true cd cyberheart && cat > public/README-UPLOAD.md << 'EOF'
# 如何上传Logo和白皮书

## 📸 Logo上传指南

1. **准备logo文件**
   - 文件名：`logo.png`
   - 建议尺寸：512x512px 或 1024x1024px
   - 格式：PNG（支持透明背景）
   - 文件大小：建议小于500KB

2. **上传到GitHub**
   ```bash
   # 在本地项目中
   git add public/logo.png
   git commit -m "Add CyberHeart logo"
   git push
   ```

3. **使用位置**
   - 导航栏左侧
   - 页脚中心
   - 浏览器标签图标（favicon）
   - 社交媒体分享图

---

## 📄 白皮书上传指南

1. **准备白皮书文件**
   - 文件名：`litepaper.pdf`
   - 格式：PDF
   - 建议大小：小于10MB

2. **上传到GitHub**
   ```bash
   # 在本地项目中
   git add public/litepaper.pdf
   git commit -m "Add CyberHeart litepaper"
   git push
   ```

3. **链接位置**
   - Hero区域的"Read Litepaper (PDF)"按钮会自动链接到此文件

---

## 🔧 如果需要修改文件名

### 修改Logo文件名（例如改为 cyht-logo.png）

cd /home/project && npm_config_yes=true cd cyberheart && cat > public/README-UPLOAD.md << 'EOF'
# 如何上传Logo和白皮书

## 📸 Logo上传指南

1. **准备logo文件**
   - 文件名：`logo.png`
   - 建议尺寸：512x512px 或 1024x1024px
   - 格式：PNG（支持透明背景）
   - 文件大小：建议小于500KB

2. **上传到GitHub**
   ```bash
   # 在本地项目中
   git add public/logo.png
   git commit -m "Add CyberHeart logo"
   git push
   ```

3. **使用位置**
   - 导航栏左侧
   - 页脚中心
   - 浏览器标签图标（favicon）
   - 社交媒体分享图

---

## 📄 白皮书上传指南

1. **准备白皮书文件**
   - 文件名：`litepaper.pdf`
   - 格式：PDF
   - 建议大小：小于10MB

2. **上传到GitHub**
   ```bash
   # 在本地项目中
   git add public/litepaper.pdf
   git commit -m "Add CyberHeart litepaper"
   git push
   ```

3. **链接位置**
   - Hero区域的"Read Litepaper (PDF)"按钮会自动链接到此文件

---

## 🔧 如果需要修改文件名

### 修改Logo文件名（例如改为 cyht-logo.png）

`src/app/layout.tsx` 和 `src/app/page.tsx`

 `/logo.png` 替换为 `/cyht-logo.png`

### 修改白皮书文件名（例如改为 whitepaper.pdf）

cd /home/project && npm_config_yes=true cd cyberheart && cat > public/README-UPLOAD.md << 'EOF'
# 如何上传Logo和白皮书

## 📸 Logo上传指南

1. **准备logo文件**
   - 文件名：`logo.png`
   - 建议尺寸：512x512px 或 1024x1024px
   - 格式：PNG（支持透明背景）
   - 文件大小：建议小于500KB

2. **上传到GitHub**
   ```bash
   # 在本地项目中
   git add public/logo.png
   git commit -m "Add CyberHeart logo"
   git push
   ```

3. **使用位置**
   - 导航栏左侧
   - 页脚中心
   - 浏览器标签图标（favicon）
   - 社交媒体分享图

---

## 📄 白皮书上传指南

1. **准备白皮书文件**
   - 文件名：`litepaper.pdf`
   - 格式：PDF
   - 建议大小：小于10MB

2. **上传到GitHub**
   ```bash
   # 在本地项目中
   git add public/litepaper.pdf
   git commit -m "Add CyberHeart litepaper"
   git push
   ```

3. **链接位置**
   - Hero区域的"Read Litepaper (PDF)"按钮会自动链接到此文件

---

## 🔧 如果需要修改文件名

### 修改Logo文件名（例如改为 cyht-logo.png）

cd /home/project && npm_config_yes=true cd cyberheart && cat > public/README-UPLOAD.md << 'EOF'
# 如何上传Logo和白皮书

## 📸 Logo上传指南

1. **准备logo文件**
   - 文件名：`logo.png`
   - 建议尺寸：512x512px 或 1024x1024px
   - 格式：PNG（支持透明背景）
   - 文件大小：建议小于500KB

2. **上传到GitHub**
   ```bash
   # 在本地项目中
   git add public/logo.png
   git commit -m "Add CyberHeart logo"
   git push
   ```

3. **使用位置**
   - 导航栏左侧
   - 页脚中心
   - 浏览器标签图标（favicon）
   - 社交媒体分享图

---

## 📄 白皮书上传指南

1. **准备白皮书文件**
   - 文件名：`litepaper.pdf`
   - 格式：PDF
   - 建议大小：小于10MB

2. **上传到GitHub**
   ```bash
   # 在本地项目中
   git add public/litepaper.pdf
   git commit -m "Add CyberHeart litepaper"
   git push
   ```

3. **链接位置**
   - Hero区域的"Read Litepaper (PDF)"按钮会自动链接到此文件

---

## 🔧 如果需要修改文件名

### 修改Logo文件名（例如改为 cyht-logo.png）

`src/app/layout.tsx` 和 `src/app/page.tsx`

 `/logo.png` 替换为 `/cyht-logo.png`

### 修改白皮书文件名（例如改为 whitepaper.pdf）

`src/app/page.tsx`

cd /home/project && npm_config_yes=true cd cyberheart && cat > public/README-UPLOAD.md << 'EOF'
# 如何上传Logo和白皮书

## 📸 Logo上传指南

1. **准备logo文件**
   - 文件名：`logo.png`
   - 建议尺寸：512x512px 或 1024x1024px
   - 格式：PNG（支持透明背景）
   - 文件大小：建议小于500KB

2. **上传到GitHub**
   ```bash
   # 在本地项目中
   git add public/logo.png
   git commit -m "Add CyberHeart logo"
   git push
   ```

3. **使用位置**
   - 导航栏左侧
   - 页脚中心
   - 浏览器标签图标（favicon）
   - 社交媒体分享图

---

## 📄 白皮书上传指南

1. **准备白皮书文件**
   - 文件名：`litepaper.pdf`
   - 格式：PDF
   - 建议大小：小于10MB

2. **上传到GitHub**
   ```bash
   # 在本地项目中
   git add public/litepaper.pdf
   git commit -m "Add CyberHeart litepaper"
   git push
   ```

3. **链接位置**
   - Hero区域的"Read Litepaper (PDF)"按钮会自动链接到此文件

---

## 🔧 如果需要修改文件名

### 修改Logo文件名（例如改为 cyht-logo.png）

cd /home/project && npm_config_yes=true cd cyberheart && cat > public/README-UPLOAD.md << 'EOF'
# 如何上传Logo和白皮书

## 📸 Logo上传指南

1. **准备logo文件**
   - 文件名：`logo.png`
   - 建议尺寸：512x512px 或 1024x1024px
   - 格式：PNG（支持透明背景）
   - 文件大小：建议小于500KB

2. **上传到GitHub**
   ```bash
   # 在本地项目中
   git add public/logo.png
   git commit -m "Add CyberHeart logo"
   git push
   ```

3. **使用位置**
   - 导航栏左侧
   - 页脚中心
   - 浏览器标签图标（favicon）
   - 社交媒体分享图

---

## 📄 白皮书上传指南

1. **准备白皮书文件**
   - 文件名：`litepaper.pdf`
   - 格式：PDF
   - 建议大小：小于10MB

2. **上传到GitHub**
   ```bash
   # 在本地项目中
   git add public/litepaper.pdf
   git commit -m "Add CyberHeart litepaper"
   git push
   ```

3. **链接位置**
   - Hero区域的"Read Litepaper (PDF)"按钮会自动链接到此文件

---

## 🔧 如果需要修改文件名

### 修改Logo文件名（例如改为 cyht-logo.png）

`src/app/layout.tsx` 和 `src/app/page.tsx`

 `/logo.png` 替换为 `/cyht-logo.png`

### 修改白皮书文件名（例如改为 whitepaper.pdf）

cd /home/project && npm_config_yes=true cd cyberheart && cat > public/README-UPLOAD.md << 'EOF'
# 如何上传Logo和白皮书

## 📸 Logo上传指南

1. **准备logo文件**
   - 文件名：`logo.png`
   - 建议尺寸：512x512px 或 1024x1024px
   - 格式：PNG（支持透明背景）
   - 文件大小：建议小于500KB

2. **上传到GitHub**
   ```bash
   # 在本地项目中
   git add public/logo.png
   git commit -m "Add CyberHeart logo"
   git push
   ```

3. **使用位置**
   - 导航栏左侧
   - 页脚中心
   - 浏览器标签图标（favicon）
   - 社交媒体分享图

---

## 📄 白皮书上传指南

1. **准备白皮书文件**
   - 文件名：`litepaper.pdf`
   - 格式：PDF
   - 建议大小：小于10MB

2. **上传到GitHub**
   ```bash
   # 在本地项目中
   git add public/litepaper.pdf
   git commit -m "Add CyberHeart litepaper"
   git push
   ```

3. **链接位置**
   - Hero区域的"Read Litepaper (PDF)"按钮会自动链接到此文件

---

## 🔧 如果需要修改文件名

### 修改Logo文件名（例如改为 cyht-logo.png）

cd /home/project && npm_config_yes=true cd cyberheart && cat > public/README-UPLOAD.md << 'EOF'
# 如何上传Logo和白皮书

## 📸 Logo上传指南

1. **准备logo文件**
   - 文件名：`logo.png`
   - 建议尺寸：512x512px 或 1024x1024px
   - 格式：PNG（支持透明背景）
   - 文件大小：建议小于500KB

2. **上传到GitHub**
   ```bash
   # 在本地项目中
   git add public/logo.png
   git commit -m "Add CyberHeart logo"
   git push
   ```

3. **使用位置**
   - 导航栏左侧
   - 页脚中心
   - 浏览器标签图标（favicon）
   - 社交媒体分享图

---

## 📄 白皮书上传指南

1. **准备白皮书文件**
   - 文件名：`litepaper.pdf`
   - 格式：PDF
   - 建议大小：小于10MB

2. **上传到GitHub**
   ```bash
   # 在本地项目中
   git add public/litepaper.pdf
   git commit -m "Add CyberHeart litepaper"
   git push
   ```

3. **链接位置**
   - Hero区域的"Read Litepaper (PDF)"按钮会自动链接到此文件

---

## 🔧 如果需要修改文件名

### 修改Logo文件名（例如改为 cyht-logo.png）

`src/app/layout.tsx` 和 `src/app/page.tsx`

 `/logo.png` 替换为 `/cyht-logo.png`

### 修改白皮书文件名（例如改为 whitepaper.pdf）

`src/app/page.tsx`


```tsx
<a href="/litepaper.pdf" target="_blank" rel="noopener noreferrer">
```


```tsx
<a href="/whitepaper.pdf" target="_blank" rel="noopener noreferrer">
```

---

## 📁 文件位置


```
cyberheart/
  public/
    ├── logo.png          ← 你的logo
    ├── litepaper.pdf     ← 你的白皮书
    └── README-UPLOAD.md  ← 本说明文件
```
